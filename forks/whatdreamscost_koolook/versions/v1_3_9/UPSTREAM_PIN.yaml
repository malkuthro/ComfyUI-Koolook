source_repo: "https://github.com/WhatDreamsCost/WhatDreamsCost-ComfyUI.git"
source_ref: "v1.3.9 (pyproject/README version on main; no v1.3.9 git tag at fork time)"
pinned_commit: "3b65410c1ce684663a3dfa6fb073588c35275d83"
external_checkout_relative_to_forks_root: "whatdreamscost-v1.3.9-koolook"
upstream_tracking_relative_to_forks_root: "whatdreamscost-main-upstream"
external_work_branch: "main"
license: "GPL-3.0"
license_verified_at: "2026-05-29"
notes: |
  This version folder ships:
    - ltx_director.py  (MODIFIED - adds `relay_overrides` multiline input
                        and keeps upstream v1.3.9 audio latent fixes)
    - prompt_relay.py  (MODIFIED - per-segment sigma per Prompt-Relay paper)
    - patches.py       (UNMODIFIED - vendored verbatim; ltx_director.py
                        imports detect_model_type / apply_patches from it)

  The canonical workflow-facing ID is `LTXDirector__koolook` so future
  compatible upstream-version upgrades can fit in place. The previous
  `LTXDirector__koolook_v1_3_2` ID remains registered as a compatibility
  alias for saved workflows, backed by this v1.3.9 implementation rather
  than byte-identical v1.3.2 code. The companion `LTXDirectorGuide` is
  unmodified and consumed from the user's upstream install.
