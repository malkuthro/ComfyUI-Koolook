_safe*
.venv/
.tmp/
Runpod_Comfy/
PLAN.md

.env
.env.local
.env.*.local

__pycache__/
*.pyc
*.pyo
*.egg-info/
.DS_Store
.pytest_cache/

# Generated audio-lipsync run cards are reproducible from each run's
# runNNN_workflow.json + notes/metadata. Keep delivery copies outside the repo.
docs/automations/LTX-2.3/audio-lipsync/runs/run-*/card.png

# Generated by `scripts/sync_to_dev.py` — written into the *target* ComfyUI
# install, never the source tree, but ignored here as a defensive guard.
web/_dev_build.json

# Local maintainer dev workspaces — not part of the published package.
# These contain vendored 3rd-party clones and debugging scratchpads kept
# only on the maintainer's disk. Anything in here would otherwise leak
# into the Comfy Registry archive (where the registry's static scanner
# would count their `NODE_CLASS_MAPPINGS` against this pack — see
# CHANGELOG entry for v0.1.4 for the full back-story).
upscaler_FIX/
nuke_CAM_exporter/
